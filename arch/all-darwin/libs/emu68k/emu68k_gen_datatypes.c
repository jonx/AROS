/* GENERATED by graft/gen-emu68k-bridge from workbench/libs/datatypes/datatypes.conf - do not edit.
 *
 * Tier 1 crossings for datatypes.library: every argument is a scalar or a pointer to
 * bytes, and the result is a scalar, so the crossing follows entirely from
 * the vector's declared prototype and register mapping. Anything needing a
 * structure rebuilt, a BPTR mapped, or a callback into the guest is NOT
 * here: those are hand-written in emu68k_oscall.c.
 */

#include <exec/types.h>
#include <proto/datatypes.h>

#include "emu68k_gen.h"

int emu68k_gen_datatypes(int lvo, struct Emu68kRegs *r, APTR guest0, APTR base)
{
    struct Library *DataTypesBase = base;
    (void)r; (void)guest0; (void)DataTypesBase;

    switch (lvo)
    {
    case 47:  /* GetDTTriggerMethodDataFlags(ULONG method) -> ULONG  [-282] */
        r->d[0] = (ULONG)GetDTTriggerMethodDataFlags((ULONG)r->a[0]);
        return 0;
    }
    return 1;   /* not a tier-1 vector of this library */
}
