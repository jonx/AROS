/* GENERATED by graft/gen-emu68k-bridge from rom/keymap/keymap.conf - do not edit.
 *
 * Derived crossings for keymap.library. Tier-1 scalar/string calls come straight
 * from the .conf prototype and register map; type-sensitive calls additionally
 * consume the checked semantic policy in graft/emu68k-bridge-policy.json.
 * Anything still lacking policy remains unavailable rather than guessed.
 */

#include <exec/types.h>
#include <proto/keymap.h>

#include "emu68k_gen.h"

int emu68k_gen_keymap(int lvo, struct Emu68kRegs *r, APTR guest0, APTR base,
                  char *err, ULONG errlen)
{
    struct Library *KeymapBase = base;
    (void)r; (void)guest0; (void)KeymapBase; (void)err; (void)errlen;

    switch (lvo)
    {
    case 5:  /* SetKeyMapDefault: no crossing [-30] */
        if (err && errlen)
            snprintf(err, errlen, "capability gap: keymap.library.SetKeyMapDefault needs struct KeyMap * described");
        r->d[0] = 0;
        return 1;
    case 6:  /* AskKeyMapDefault: no crossing [-36] */
        if (err && errlen)
            snprintf(err, errlen, "capability gap: keymap.library.AskKeyMapDefault needs returns a pointer (struct KeyMap *) described");
        r->d[0] = 0;
        return 1;
    case 7:  /* MapRawKey: no crossing [-42] */
        if (err && errlen)
            snprintf(err, errlen, "capability gap: keymap.library.MapRawKey needs struct InputEvent *, struct KeyMap * described");
        r->d[0] = 0;
        return 1;
    case 8:  /* MapANSI: no crossing [-48] */
        if (err && errlen)
            snprintf(err, errlen, "capability gap: keymap.library.MapANSI needs struct KeyMap * described");
        r->d[0] = 0;
        return 1;
    }
    return 1;   /* no safe generated crossing for this vector */
}
