/* GENERATED by graft/gen-emu68k-bridge from workbench/libs/workbench/workbench.conf - do not edit.
 *
 * Derived crossings for workbench.library. Tier-1 scalar/string calls come straight
 * from the .conf prototype and register map; type-sensitive calls additionally
 * consume the checked semantic policy in graft/emu68k-bridge-policy.json.
 * Anything still lacking policy remains unavailable rather than guessed.
 */

#include <exec/types.h>
#include <proto/workbench.h>
#include <string.h>

#include "emu68k_gen.h"
#include "emu68k_layouts.h"

int emu68k_gen_workbench(int lvo, struct Emu68kRegs *r, APTR guest0, APTR base,
                  char *err, ULONG errlen)
{
    struct Library *WorkbenchBase = base;
    (void)r; (void)guest0; (void)WorkbenchBase; (void)err; (void)errlen;

    switch (lvo)
    {
    case 5:  /* UpdateWorkbench(CONST_STRPTR name, BPTR lock, LONG action) -> BOOL  [-30] */
        r->d[0] = (ULONG)UpdateWorkbench((CONST_STRPTR)EMU_GPTR(guest0, r->a[0]),
              EMU_HANDLE(guest0, r->a[1]),
              (LONG)r->d[0]);
        return 0;
    case 6:  /* QuoteWorkbench(ULONG stringNum) -> BOOL  [-36] */
        r->d[0] = (ULONG)QuoteWorkbench((ULONG)r->d[0]);
        return 0;
    case 14:  /* WBConfig(ULONG unk1, ULONG unk2) -> BOOL  [-84] */
        r->d[0] = (ULONG)WBConfig((ULONG)r->d[0],
              (ULONG)r->d[1]);
        return 0;
    case 24:  /* UnregisterWorkbench(struct MsgPort * messageport) -> BOOL  [-144] */
    {
        APTR emu_object_0;
        if (emu68k_object_from_guest(guest0, r->a[0], EMU_OBJ_MsgPort, 1,
                                        "MsgPort", &emu_object_0, err, errlen) < 0)
            return 1;
            r->d[0] = (ULONG)UnregisterWorkbench((struct MsgPort *)emu_object_0);
            return 0;
    }
    }
    return 1;   /* no safe generated crossing for this vector */
}
