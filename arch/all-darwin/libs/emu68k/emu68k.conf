##begin config
basename Emu68k
libbase Emu68kBase
libbasetype struct Emu68kBase
version 1.0
##end config

##begin cdef
#include <libraries/emu68k.h>
##end cdef

##begin cdefprivate
#include "emu68k_intern.h"
##end cdefprivate

##begin functionlist
# FROZEN SLOT: dos.library's launch router calls Emu68k_RunSeg by LVO number
# (5 = this first vector) without proto headers. Never reorder; append only.
LONG Emu68k_RunSeg(struct Emu68kLaunchCtx *ctx, LONG *result) (A0, A1)
##end functionlist
